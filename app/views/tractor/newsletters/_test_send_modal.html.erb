<div class="modal hide fade" id="myModal">
  <div class="modal-header">
    <button class="close" data-dismiss="modal">×</button>
    <h3>Send a test of email</h3>
  </div>
  <div class="modal-body">
    <p>Enter comma separated email addresses to send test to:</p>
    <%= simple_form_for @newsletter_test_email, :remote => true do |f| %>
      <%= f.input :send_to, :style => "width: 100%" %>
      <%= f.input :newsletter_id, :input_html => { :value => @newsletter.id }, :as => 'hidden' %>
      
    </div>
    <div class="modal-footer">
      <a href="#" class="btn">Close</a>
      <%= f.button :submit, :class => 'btn btn-info' %>

    <% end %>
  </div>
</div>
